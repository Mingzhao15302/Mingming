{
  "name": "huicloud-os",
  "version": "25.11.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "start": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "lint": "npm run lint --workspace web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
